# Builder resources for electron-builder — place icons here:
#   build/icon.ico      (Windows; required for build:win)
#   build/icon.icns     (macOS; required for build:mac)
#   build/icon.png      (Linux, 512x512)
#
# If these files are missing, electron-builder falls back to its default icon.
